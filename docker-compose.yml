services:
  api:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    command: python -m uvicorn api.main:app --host 0.0.0.0 --port 8000

  app:
    build: .
    ports:
      - "127.0.0.1:8501:8501"
    command: python -m streamlit run mvp/app.py --server.address 0.0.0.0
